# Security policy

Do not open a public issue containing credentials, tokens, private media,
database records, or production logs. Report sensitive vulnerabilities through
the repository owner's private contact channel.

This repository intentionally excludes production domains, databases, uploads,
environment files, signing keys, R8 mappings, SMTP credentials, Turnstile
secrets, administrator credentials, and active sessions.
